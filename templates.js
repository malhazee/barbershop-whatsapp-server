// رسالة إشعار الحلاق بحجز جديد
function getBarberNewBooking(name, date, time, service) {
    return `✂️ حجز جديد

العميل: *${name}*
الخدمة: *${service}*
التاريخ: *${date}*
الوقت: *${time}*`;
}

// رسالة إشعار الحلاق بإلغاء حجز
function getBarberCancellation(name, date, time, service) {
    return `⚠️ تم إلغاء حجز

العميل: *${name}*
الخدمة: *${service}*
التاريخ: *${date}*
الوقت: *${time}*`;
}

// رسالة كود التحقق OTP
function getOTPMessage(code) {
    return `*نظام التحقق الآمن* 🔐

السيد/ة العميل المحترم،

كود التحقق الخاص بحجزكم:

▶️ *${code}*

⏱ صلاحية الكود: *5 دقائق*

⚠️ *تنبيه أمني مهم:*
• هذا الكود سري ولا يجب مشاركته
• لم نطلبه؟ يُرجى تجاهل هذه الرسالة
• للاستفسارات: تواصل معنا مباشرة

━━━━━━━━━━━━━━━━
نظام الحجز الإلكتروني`;
}

// ========================================
// قوالب رسائل واتساب
// ========================================

// رسالة تأكيد الحجز
function getBookingConfirmation(name, date, time, service) {
    return `*تأكيد حجز موعد* ✅

السيد/ة *${name}* المحترم/ة،

تم تأكيد حجزكم بنجاح

━━━━━━━━━━━━━━━━━━
📅 *التاريخ:* ${date}
🕐 *الوقت:* ${time}
✂️ *الخدمة:* ${service}
━━━━━━━━━━━━━━━━━━

*ملاحظات هامة:*
• يُرجى الحضور قبل الموعد بـ 5 دقائق
• سيتم إرسال تذكير قبل الموعد
• للإلغاء أو التعديل: استخدم رابط الحجز

نسعد بخدمتكم 🙏`;
}

// رسالة تذكير قبل الموعد
function getReminderMessage(name, date, time) {
    return `*تذكير بالموعد* 🔔

السيد/ة *${name}* المحترم/ة،

نذكّركم بموعدكم المحجوز

━━━━━━━━━━━━━━━━━━
📅 التاريخ: *${date}*
🕐 الوقت: *${time}*
━━━━━━━━━━━━━━━━━━

*في حال تعذر الحضور:*
يُرجى الإلغاء عبر رابط الحجز لإتاحة المعاد لعميل آخر

نسعد بخدمتكم`;
}

// رسالة تذكير قبل ساعتين
function getReminderMessage2Hours(name, time) {
    return `*تذكير: موعدكم بعد ساعتين* ⏰

السيد/ة *${name}*،

موعدكم في تمام الساعة: *${time}*

نتطلع لاستقبالكم`;
}

// رسالة تذكير قبل ساعة
function getReminderMessage1Hour(name, time) {
    return `*تذكير: موعدكم بعد ساعة* ⏰

السيد/ة *${name}*،

موعدكم في تمام الساعة: *${time}*

يُرجى التوجه للصالون في الوقت المحدد`;
}

// رسالة تذكير قبل 15 دقيقة
function getReminderMessage15Min(name, time) {
    return `*تذكير نهائي* ⏳

السيد/ة *${name}*،

موعدكم بعد *15 دقيقة*
الساعة: *${time}*

في انتظاركم`;
}

// رسالة إلغاء الموعد
function getCancellationMessage(name, date, time, reason, websiteUrl = 'https://barbershop-appointments-533ce.web.app') {
    return `*إشعار إلغاء موعد* ❌

السيد/ة *${name}* المحترم/ة،

تم إلغاء موعدكم المحجوز

━━━━━━━━━━━━━━━━━━
📅 التاريخ: ${date}
🕐 الوقت: ${time}

💬 السبب: ${reason}
━━━━━━━━━━━━━━━━━━

*لحجز موعد جديد:*
${websiteUrl}

نعتذر عن أي إزعاج`;
}

// رسالة إشعار الحلاق بحجز جديد
function getBarberNewBooking(name, date, time, service) {
    return `*حجز جديد في النظام* 📋

━━━━━━━━━━━━━━━━━━
👤 العميل: *${name}*
✂️ الخدمة: *${service}*
📅 التاريخ: *${date}*
🕐 الوقت: *${time}*
━━━━━━━━━━━━━━━━━━

يُرجى التأكد من الجاهزية`;
}

// رسالة إشعار الحلاق بإلغاء حجز
function getBarberCancellation(name, date, time, service) {
    return `*إلغاء حجز* ⚠️

━━━━━━━━━━━━━━━━━━
👤 العميل: *${name}*
✂️ الخدمة: *${service}*
📅 التاريخ: *${date}*
🕐 الوقت: *${time}*
━━━━━━━━━━━━━━━━━━

تم تحرير هذا الموعد`;
}

// ========================================
// Exports
// ========================================

module.exports = {
    getBookingConfirmation,
    getReminderMessage,
    getReminderMessage2Hours,
    getReminderMessage1Hour,
    getReminderMessage15Min,
    getCancellationMessage,
    getBarberNewBooking,
    getBarberCancellation,
    getOTPMessage
};
