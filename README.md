# WhatsApp Server for Barbershop Appointments

نظام إرسال إشعارات واتساب تلقائية لنظام حجز مواعيد الحلاقة.

## 📋 المتطلبات

- Node.js v16 أو أحدث
- npm أو yarn
- رقم واتساب (يُفضل رقم منفصل للأعمال)

## 🚀 التثبيت

### 1. تثبيت المكتبات

```bash
npm install
```

### 2. إعداد ملف البيئة

```bash
cp .env.example .env
```

### 3. تشغيل السيرفر

```bash
npm start
```

## 📱 ربط واتساب

1. شغّل السيرفر بالأمر `npm start`
2. افتح المتصفح على `http://localhost:3000`
3. سيظهر لك QR Code
4. افتح واتساب على هاتفك
5. اذهب إلى: **الإعدادات** → **الأجهزة المرتبطة** → **ربط جهاز**
6. امسح ال QR Code
7. تم! ✅

## 🔌 API Endpoints

### 1. حالة السيرفر
```
GET /status
```

**Response:**
```json
{
  "isReady": true,
  "hasQRCode": false,
  "timestamp": "2025-11-16T..."
}
```

---

### 2. تأكيد الحجز
```
POST /send-booking-confirmation
```

**Body:**
```json
{
  "phone": "0791234567",
  "name": "أحمد",
  "date": "2025-11-20",
  "time": "14:00",
  "service": "حلاقة شعر + لحية"
}
```

---

### 3. تذكير بالموعد
```
POST /send-reminder
```

**Body:**
```json
{
  "phone": "0791234567",
  "name": "أحمد",
  "date": "2025-11-20",
  "time": "14:00"
}
```

---

### 4. إشعار الإلغاء
```
POST /send-cancellation
```

**Body:**
```json
{
  "phone": "0791234567",
  "name": "أحمد",
  "date": "2025-11-20",
  "time": "14:00",
  "reason": "ظرف طارئ",
  "websiteUrl": "https://your-site.com"
}
```

---

### 5. رسالة شكر
```
POST /send-thankyou
```

**Body:**
```json
{
  "phone": "0791234567",
  "name": "أحمد"
}
```

---

### 6. إعادة تعيين الاتصال
```
POST /reset
```

## 📝 ملاحظات مهمة

### أرقام الهواتف
- يتم تنسيق الأرقام تلقائياً
- يُضاف كود الأردن (+962) إذا لم يكن موجوداً
- مثال: `0791234567` → `962791234567@c.us`

### الأمان
- لا تشارك ملف `.wwebjs_auth` (يحتوي على جلسة واتساب)
- استخدم رقم منفصل للأعمال
- احتفظ بنسخة احتياطية من السيرفر

### الأداء
- السيرفر يدعم إرسال آلاف الرسائل
- لكن انتبه لسياسة واتساب ضد الرسائل المزعجة
- يُفضل عدم إرسال أكثر من 100 رسالة/ساعة

## 🔧 حل المشاكل

### المشكلة: QR Code لا يظهر
**الحل:** 
- أعد تشغيل السيرفر
- احذف مجلد `.wwebjs_auth`
- تأكد من تثبيت جميع المكتبات

### المشكلة: فشل إرسال الرسالة
**الحل:**
- تأكد من أن واتساب متصل
- تحقق من صحة رقم الهاتف
- تأكد من أن الرقم مسجل في واتساب

### المشكلة: قطع الاتصال المتكرر
**الحل:**
- استخدم اتصال إنترنت مستقر
- لا تستخدم نفس الرقم على أجهزة متعددة
- تأكد من عدم تسجيل خروج من الهاتف

## 🚀 نشر السيرفر (Production)

### الخيار 1: VPS (مثل DigitalOcean)
```bash
# على السيرفر
git clone your-repo
cd whatsapp-server
npm install
npm start
```

### الخيار 2: Heroku
```bash
heroku create
git push heroku main
heroku ps:scale web=1
```

### الخيار 3: Railway
- ارفع المشروع على GitHub
- اربطه مع Railway
- سيعمل تلقائياً!

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
1. الـ Console لمعرفة الأخطاء
2. حالة الاتصال على `/status`
3. ملف `.wwebjs_auth` موجود

## 📄 الترخيص

MIT License - استخدمه بحرية!
